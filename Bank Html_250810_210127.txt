<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Luchukomol – Photo & Dance Portfolio</title>
    <meta name="description" content="Luchukomol's official portfolio showcasing photography projects, dance concepts, and creative content creation skills.">
    <meta name="keywords" content="Luchukomol, photography, dance, portfolio, creative professional, social media, content creator, photo shoots, dance covers, TikTok, Behance, Instagram">
    <meta name="author" content="Luchukomol">

    <!-- Favicon (Optional but Recommended) -->
    <!-- <link rel="icon" href="/favicon.ico" type="image/x-icon"> -->

    <!-- Suggested CDN: Bootstrap CSS for responsive layout and enhanced UI/UX -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    
    <!-- Suggested CDN: Font Awesome for icons -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css" rel="stylesheet">
    
    <!-- Suggested CDN: Google Fonts for modern typography -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&family=Montserrat:wght@700;900&display=swap" rel="stylesheet" crossorigin="anonymous">
    
    <!-- Suggested CDN: Animate.css for easy animations in hero section -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" rel="stylesheet">
    
    <!-- Suggested CDN: AOS (Animate On Scroll) for subtle section animations -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.css" rel="stylesheet">
    
    <style>
        /* General Body Styles */
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f8f9fa; /* Light background */
            color: #333; /* Dark text for readability */
            line-height: 1.6;
            scroll-behavior: smooth; /* Smooth scrolling for anchor links */
            overflow-x: hidden; /* Prevent horizontal scroll */
        }

        /* Hero Section Styling */
        .hero {
            background: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('https://mir-s3-cdn-cf.behance.net/project_modules/source/bb00aa223013739.67f1333a98ada.jpg') center/cover no-repeat;
            background-attachment: fixed; /* Parallax effect for modern feel */
            color: #fff; /* White text for contrast against dark overlay */
            text-align: center;
            padding: 200px 20px; /* Taller hero for immersive feel */
            position: relative;
            overflow: hidden;
            min-height: 100vh; /* Full viewport height */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }
        .hero h1 {
            font-family: 'Montserrat', sans-serif;
            font-size: 4rem; /* Bolder, larger for modern impact */
            font-weight: 900;
            letter-spacing: 2px;
            margin-bottom: 15px; /* Spacing below main title */
            text-shadow: 2px 2px 4px rgba(0,0,0,0.5); /* Enhanced text visibility */
            transition: transform 0.3s ease, text-shadow 0.3s ease;
        }
        .hero h1:hover {
            transform: scale(1.02);
            text-shadow: 3px 3px 6px rgba(0,0,0,0.7);
        }
        .hero p {
            font-size: 1.8rem;
            margin-bottom: 30px;
            text-shadow: 1px 1px 3px rgba(0,0,0,0.5);
            transition: transform 0.3s ease;
        }
        .hero p:hover {
            transform: translateY(-2px);
        }

        /* Call-to-Action Button with Enhanced Hover */
        .btn-primary {
            background: linear-gradient(135deg, #00bfff, #0099cc); /* Gradient background */
            border: none;
            padding: 12px 24px;
            font-size: 1.2rem;
            font-weight: 600; /* Bolder text for CTA */
            border-radius: 50px; /* Pill shape for modern button */
            position: relative;
            overflow: hidden;
            transition: all 0.3s ease;
            box-shadow: 0 4px 15px rgba(0, 191, 255, 0.3);
        }
        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
            transition: left 0.6s ease;
        }
        .btn-primary:hover {
            background: linear-gradient(135deg, #008b8b, #006666);
            transform: translateY(-3px);
            box-shadow: 0 8px 25px rgba(0, 191, 255, 0.4);
        }
        .btn-primary:hover::before {
            left: 100%;
        }
        .btn-primary:active {
            transform: translateY(-1px);
        }

        /* Section General Styling with Enhanced Hover */
        section {
            padding: 80px 20px; /* More spacing for modern whitespace */
            transition: background-color 0.3s ease;
        }
        /* Alternating background colors for visual separation */
        section:nth-child(even) { /* About, Gallery, Skills sections will be light gray */
            background-color: #f2f2f2;
        }
        section:nth-child(odd) { /* Photography Projects, Dance, Connect sections will be white */
            background-color: #ffffff;
        }
        section:hover {
            background-color: rgba(0, 191, 255, 0.02); /* Subtle hover effect on sections */
        }

        /* Section Titles with Enhanced Hover */
        .section-title {
            font-family: 'Montserrat', sans-serif;
            text-align: center;
            margin-bottom: 50px;
            font-size: 3rem;
            font-weight: 700;
            color: #00bfff; /* Accent color for headings */
            position: relative;
            padding-bottom: 15px;
            transition: all 0.3s ease;
            cursor: default;
        }
        .section-title:hover {
            transform: translateY(-3px);
            text-shadow: 0 4px 8px rgba(0, 191, 255, 0.3);
        }
        .section-title::after { /* Underline effect for titles */
            content: '';
            position: absolute;
            left: 50%;
            bottom: 0;
            transform: translateX(-50%);
            width: 80px;
            height: 4px;
            background: linear-gradient(90deg, #00bfff, #0099cc);
            border-radius: 2px;
            transition: width 0.3s ease;
        }
        .section-title:hover::after {
            width: 120px;
        }

        /* Card Styling with Enhanced Hover Effects */
        .card {
            background-color: #ffffff; /* White cards */
            border: 1px solid #e0e0e0; /* Subtle border for definition */
            border-radius: 12px;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275); /* Elastic transition */
            color: #333; /* Dark text inside cards */
            height: 100%; /* Ensures cards in a row have equal height */
            overflow: hidden; /* For featured project images */
            position: relative;
        }
        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, rgba(0, 191, 255, 0.1), rgba(0, 139, 139, 0.1));
            opacity: 0;
            transition: opacity 0.3s ease;
            z-index: 1;
        }
        .card:hover {
            transform: translateY(-20px) scale(1.02);
            box-shadow: 0 20px 40px rgba(0, 191, 255, 0.15); /* Enhanced blue glow */
            border-color: #00bfff;
        }
        .card:hover::before {
            opacity: 1;
        }
        .card-body {
            position: relative;
            z-index: 2;
            transition: transform 0.3s ease;
        }
        .card:hover .card-body {
            transform: translateY(-5px);
        }
        .card-title {
            color: #00bfff; /* Accent color for card titles */
            font-weight: 600;
            transition: color 0.3s ease, transform 0.3s ease;
        }
        .card:hover .card-title {
            color: #008b8b;
            transform: scale(1.05);
        }
        .card-subtitle {
            font-size: 0.9em;
            transition: color 0.3s ease;
        }
        .card:hover .card-subtitle {
            color: #666;
        }

        /* Featured Projects with Advanced Hover Effects */
        .featured-project-card {
            position: relative;
            overflow: hidden;
            cursor: pointer;
        }
        .featured-project-card .card-img-top {
            height: 250px;
            object-fit: cover;
            transition: all 0.4s ease;
            filter: brightness(0.9);
        }
        .featured-project-card:hover .card-img-top {
            transform: scale(1.08) rotate(1deg);
            filter: brightness(1.1);
        }
        .featured-project-card .card-body {
            position: relative;
            z-index: 2;
        }
        .featured-project-card .project-overlay {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(45deg, rgba(0,191,255,0.1) 0%, rgba(0,0,0,0.3) 50%, rgba(0,139,139,0.2) 100%);
            opacity: 0;
            transition: all 0.4s ease;
        }
        .featured-project-card:hover .project-overlay {
            opacity: 1;
        }
        .project-link {
            text-decoration: none;
            color: inherit;
            display: block;
            transition: transform 0.3s ease;
        }
        .project-link:hover {
            color: inherit;
            transform: scale(1.01);
        }
        .project-badge {
            position: absolute;
            top: 15px;
            right: 15px;
            background: linear-gradient(135deg, #00bfff, #008b8b);
            color: white;
            padding: 8px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
            transition: all 0.3s ease;
            z-index: 3;
        }
        .featured-project-card:hover .project-badge {
            transform: scale(1.1) translateY(-2px);
            box-shadow: 0 4px 12px rgba(0, 191, 255, 0.4);
        }

        /* Skills Grid with Responsive Design */
        .skills-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 30px; /* More gap for breathing room */
        }

        /* Connect Links with Enhanced Hover */
        .connect-links {
            display: flex;
            flex-wrap: wrap; /* Allow wrapping on small screens */
            justify-content: center;
            gap: 30px;
        }
        .connect-links a {
            font-size: 1.4rem;
            color: #007bff; /* Standard blue for links on light background */
            text-decoration: none;
            transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            display: flex; /* For icon and text alignment */
            align-items: center;
            padding: 10px 20px;
            border-radius: 25px;
            background: rgba(0, 123, 255, 0.1);
            position: relative;
            overflow: hidden;
        }
        .connect-links a::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.4), transparent);
            transition: left 0.5s ease;
        }
        .connect-links a i {
            margin-right: 8px; /* Space between icon and text */
            transition: transform 0.3s ease;
        }
        .connect-links a:hover {
            color: #fff;
            background: linear-gradient(135deg, #007bff, #0056b3);
            transform: translateY(-5px) scale(1.05);
            box-shadow: 0 8px 25px rgba(0, 123, 255, 0.3);
        }
        .connect-links a:hover::before {
            left: 100%;
        }
        .connect-links a:hover i {
            transform: scale(1.2) rotate(5deg);
        }

        /* List Group Items with Hover Effects */
        .list-group-item {
            background-color: #ffffff; /* White list items */
            color: #333; /* Dark text */
            border: 1px solid #e0e0e0; /* Subtle border */
            border-radius: 8px;
            margin-bottom: 10px;
            padding: 15px 20px; /* More padding */
            font-size: 1.1rem;
            transition: all 0.3s ease;
            cursor: default;
            position: relative;
            overflow: hidden;
        }
        .list-group-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, rgba(0,191,255,0.1), rgba(0,139,139,0.1));
            transition: left 0.4s ease;
        }
        .list-group-item:hover {
            transform: translateX(10px);
            border-color: #00bfff;
            box-shadow: 0 4px 15px rgba(0, 191, 255, 0.15);
        }
        .list-group-item:hover::before {
            left: 0;
        }

        /* Carousel with Enhanced Hover */
        #galleryCarousel .carousel-item img {
            border-radius: 12px; /* Rounded corners for gallery images */
            box-shadow: 0 8px 20px rgba(0,0,0,0.1); /* Subtle shadow for depth */
            transition: all 0.4s ease;
        }
        #galleryCarousel:hover .carousel-item img {
            transform: scale(1.02);
            box-shadow: 0 15px 30px rgba(0,0,0,0.15);
        }
        #galleryCarousel .carousel-caption {
            background: rgba(0,0,0,0.6); /* Slightly darker caption background */
            border-radius: 8px;
            padding: 15px;
            bottom: 20px; /* Lift caption slightly */
            transition: all 0.3s ease;
        }
        #galleryCarousel:hover .carousel-caption {
            background: rgba(0,0,0,0.8);
            transform: translateY(-5px);
        }
        #galleryCarousel .carousel-control-prev,
        #galleryCarousel .carousel-control-next {
            transition: all 0.3s ease;
        }
        #galleryCarousel .carousel-control-prev:hover,
        #galleryCarousel .carousel-control-next:hover {
            background: rgba(0,191,255,0.1);
        }

        /* Footer with Hover Effect */
        footer {
            background-color: #333; /* Dark footer for strong contrast */
            color: #fff; /* White text on dark footer */
            text-align: center;
            padding: 30px;
            font-style: italic;
            font-size: 1.2rem;
            transition: background-color 0.3s ease;
        }
        footer:hover {
            background-color: #2a2a2a;
        }

        /* Enhanced Media Queries for Better Responsiveness */
        @media (max-width: 1200px) { /* Large devices */
            .hero h1 {
                font-size: 3.8rem;
            }
            .hero p {
                font-size: 1.7rem;
            }
            .section-title {
                font-size: 2.9rem;
            }
        }
        @media (max-width: 992px) { /* Medium devices */
            .hero {
                padding: 120px 15px;
            }
            .hero h1 {
                font-size: 3.5rem;
            }
            .hero p {
                font-size: 1.6rem;
            }
            .section-title {
                font-size: 2.8rem;
                margin-bottom: 40px;
            }
            .featured-project-card .card-img-top {
                height: 220px;
            }
            .connect-links {
                gap: 20px;
            }
        }
        @media (max-width: 768px) { /* Small devices */
            .hero {
                padding: 100px 15px;
                background-attachment: scroll; /* Disable parallax on mobile for performance */
                min-height: 80vh;
            }
            .hero h1 {
                font-size: 3rem;
                letter-spacing: 1px;
            }
            .hero p {
                font-size: 1.4rem;
            }
            .section-title {
                font-size: 2.5rem;
                margin-bottom: 30px;
            }
            section {
                padding: 60px 15px;
            }
            .skills-grid {
                grid-template-columns: 1fr; /* Stack skills cards vertically */
                gap: 20px;
            }
            .featured-project-card .card-img-top {
                height: 200px;
            }
            .connect-links a {
                font-size: 1.2rem;
                padding: 8px 16px;
            }
            .card:hover {
                transform: translateY(-10px) scale(1.01); /* Reduced hover effect on mobile */
            }
        }
        @media (max-width: 576px) { /* Extra small devices */
            .hero {
                padding: 80px 15px;
                min-height: 70vh;
            }
            .hero h1 {
                font-size: 2.5rem;
                letter-spacing: 0.5px;
            }
            .hero p {
                font-size: 1.1rem;
                margin-bottom: 20px;
            }
            .btn-primary {
                font-size: 1rem;
                padding: 10px 20px;
            }
            .section-title {
                font-size: 2rem;
                margin-bottom: 25px;
            }
            section {
                padding: 40px 10px;
            }
            .connect-links {
                flex-direction: column; /* Stack connect links vertically */
                gap: 15px;
            }
            .connect-links a {
                justify-content: center;
                width: 100%;
                max-width: 280px;
            }
            .featured-project-card .card-img-top {
                height: 180px;
            }
            .list-group-item {
                padding: 12px 15px;
                font-size: 1rem;
            }
            .list-group-item:hover {
                transform: translateX(5px); /* Reduced hover effect on mobile */
            }
            .card-title {
                font-size: 1.1rem;
            }
        }
        @media (max-width: 400px) { /* Very small devices */
            .hero h1 {
                font-size: 2.2rem;
            }
            .hero p {
                font-size: 1rem;
            }
            .section-title {
                font-size: 1.8rem;
            }
            .featured-project-card .card-img-top {
                height: 160px;
            }
        }

        /* Touch device optimizations */
        @media (hover: none) and (pointer: coarse) {
            .card:hover,
            .btn-primary:hover,
            .connect-links a:hover {
                transform: none; /* Disable transform effects on touch devices */
            }
        }

        /* High DPI displays */
        @media (-webkit-min-device-pixel-ratio: 2), (min-resolution: 192dpi) {
            .hero {
                background-image: linear-gradient(rgba(0, 0, 0, 0.6), rgba(0, 0, 0, 0.6)), url('https://mir-s3-cdn-cf.behance.net/project_modules/source/bb00aa223013739.67f1333a98ada.jpg');
            }
        }

        /* Prefers reduced motion */
        @media (prefers-reduced-motion: reduce) {
            .hero {
                background-attachment: scroll;
            }
            * {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
        }
    </style>
</head>
<body>
    <!-- Animated Hero Section -->
    <header class="hero" data-aos="fade-up" data-aos-duration="1000">
        <h1 class="animate__animated animate__fadeInDown">luchukomol</h1>
        <p class="animate__animated animate__fadeInDown animate__delay-1s"><i class="fas fa-camera"></i> Luchukomol – My Photo & Dance Portfolio</p>
        <p class="animate__animated animate__fadeInDown animate__delay-2s">Creative Space for Photography Projects & Dance Ideas</p>
        <a href="#about" class="btn btn-primary animate__animated animate__fadeInUp animate__delay-3s">Explore My Work</a>
    </header>

    <!-- About Section -->
    <section id="about">
        <h2 class="section-title" data-aos="fade-up"><i class="fas fa-star"></i> About Me</h2>
        <div class="container" data-aos="fade-up" data-aos-delay="200">
            <div class="row justify-content-center">
                <div class="col-md-8 text-center">
                    <p>Hi! I'm Luchukomol – a creative professional blending photography, dance, and social media storytelling.</p>
                    <p>Here you'll find a curated selection of my photography projects, dance concepts, and creative experiments.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Featured Projects Section -->
    <section id="featured-projects">
        <h2 class="section-title" data-aos="fade-up"><i class="fas fa-trophy"></i> Featured Projects</h2>
        <div class="container">
            <div class="row" data-aos="fade-up" data-aos-delay="200">
                <div class="col-lg-6 col-md-12 mb-4">
                    <a href="https://www.behance.net/gallery/227629025/TIDE-Bang-Saray" target="_blank" rel="noopener noreferrer" class="project-link">
                        <div class="card featured-project-card">
                            <img src="https://mir-s3-cdn-cf.behance.net/project_modules/source/fd5d72227629025.6843515be3167.jpg" class="card-img-top" alt="TIDE Bang Saray Photography Project" loading="lazy">
                            <div class="project-overlay"></div>
                            <div class="project-badge">📸 Photography</div>
                            <div class="card-body">
                                <h5 class="card-title">TIDE Bang Saray</h5>
                                <p class="card-subtitle mb-2 text-muted">Commercial Photography</p>
                                <p class="card-text">A stunning photography series capturing the essence of coastal beauty and modern lifestyle at Bang Saray.</p>
                                <small class="text-muted"><i class="fab fa-behance"></i> View on Behance</small>
                            </div>
                        </div>
                    </a>
                </div>
                <div class="col-lg-6 col-md-12 mb-4">
                    <a href="https://www.behance.net/gallery/223013739/WANAREE-%28%29-The-Art-of-Thai-Japanese-Dessert" target="_blank" rel="noopener noreferrer" class="project-link">
                        <div class="card featured-project-card">
                            <img src="https://mir-s3-cdn-cf.behance.net/project_modules/source/0fe1db223013739.67f1333a9abb2.jpg" class="card-img-top" alt="WANAREE Thai Japanese Dessert Photography" loading="lazy">
                            <div class="project-overlay"></div>
                            <div class="project-badge">🍰 Food Photography</div>
                            <div class="card-body">
                                <h5 class="card-title">WANAREE (和菓) - The Art of Thai-Japanese Dessert</h5>
                                <p class="card-subtitle mb-2 text-muted">Food & Lifestyle Photography</p>
                                <p class="card-text">An artistic exploration of fusion desserts, blending Thai and Japanese culinary traditions through visual storytelling.</p>
                                <small class="text-muted"><i class="fab fa-behance"></i> View on Behance</small>
                            </div>
                        </div>
                    </a>
                </div>
            </div>
        </div>
    </section>

    <!-- Photography Projects Section -->
    <section id="photography">
        <h2 class="section-title" data-aos="fade-up"><i class="fas fa-camera-retro"></i> Photography Projects</h2>
        <div class="container">
            <div class="row" data-aos="fade-up" data-aos-delay="200">
                <div class="col-lg-6 col-md-12 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Cafe Shoot</h5>
                            <p class="card-subtitle mb-2 text-muted">Mar 2025</p>
                            <p class="card-text">Lifestyle & coffee culture photography, capturing moments in vibrant cafe settings.</p>
                        </div>
                    </div>
                </div>
                <div class="col-lg-6 col-md-12 mb-4">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">Dance Studio</h5>
                            <p class="card-subtitle mb-2 text-muted">Feb 2025</p>
                            <p class="card-text">Dynamic portraits & motion shots, utilizing studio lighting to emphasize movement.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Automated Gallery Section -->
    <section id="gallery">
        <h2 class="section-title" data-aos="fade-up"><i class="fas fa-images"></i> Gallery</h2>
        <div class="container" data-aos="fade-up" data-aos-delay="200">
            <div id="galleryCarousel" class="carousel slide" data-bs-ride="carousel" data-bs-interval="5000">
                <div class="carousel-indicators">
                    <button type="button" data-bs-target="#galleryCarousel" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
                    <button type="button" data-bs-target="#galleryCarousel" data-bs-slide-to="1" aria-label="Slide 2"></button>
                    <button type="button" data-bs-target="#galleryCarousel" data-bs-slide-to="2" aria-label="Slide 3"></button>
                </div>
                <div class="carousel-inner">
                    <div class="carousel-item active">
                        <img src="https://mir-s3-cdn-cf.behance.net/project_modules/source/0fe1db223013739.67f1333a9abb2.jpg" class="d-block w-100" alt="Thai Japanese dessert photography showcasing artistic plating" loading="lazy">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>Culinary Artistry</h5>
                            <p>The delicate beauty of Thai-Japanese dessert fusion.</p>
                        </div>
                    </div>
                    <div class="carousel-item">
                        <img src="https://mir-s3-cdn-cf.behance.net/project_modules/source/fd5d72227629025.6843515be3167.jpg" class="d-block w-100" alt="Coastal lifestyle photography at Bang Saray" loading="lazy">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>Coastal Elegance</h5>
                            <p>Capturing the serene beauty of seaside living.</p>
                        </div>
                    </div>
                    <div class="carousel-item">
                        <img src="https://mir-s3-cdn-cf.behance.net/project_modules/source/137681227629025.6843515be7bb3.jpg" class="d-block w-100" alt="Modern lifestyle photography with natural lighting" loading="lazy">
                        <div class="carousel-caption d-none d-md-block">
                            <h5>Natural Moments</h5>
                            <p>Authentic lifestyle captured through natural light.</p>
                        </div>
                                    </div>
                <button class="carousel-control-prev" type="button" data-bs-target="#galleryCarousel" data-bs-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Previous</span>
                </button>
                <button class="carousel-control-next" type="button" data-bs-target="#galleryCarousel" data-bs-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="visually-hidden">Next</span>
                </button>
            </div>
        </div>
    </section>

    <!-- Dance & Social Media Ideas Section -->
    <section id="dance">
        <h2 class="section-title" data-aos="fade-up"><i class="fas fa-music"></i> Dance & Social Media Ideas</h2>
        <div class="container" data-aos="fade-up" data-aos-delay="200">
            <div class="row justify-content-center">
                <div class="col-md-8">
                    <ul class="list-group">
                        <li class="list-group-item">🌸 TikTok challenge: #FlowerStep - A unique and trending dance challenge.</li>
                        <li class="list-group-item">🎷 Dance cover: "Smooth Operator" – Jazz style - A sophisticated and smooth performance.</li>
                        <li class="list-group-item">📸 Photo set: Black & Yellow fashion shoot - High-contrast fashion photography concept.</li>
                    </ul>
                </div>
            </div>
        </div>
    </section>

    <!-- Skills & Tools Section -->
    <section id="skills">
        <h2 class="section-title" data-aos="fade-up"><i class="fas fa-tools"></i> Skills & Tools</h2>
        <div class="container skills-grid" data-aos="fade-up" data-aos-delay="200">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-camera"></i> Photography</h5>
                    <p>Sony A7 III, Sony A7C, Adobe Lightroom & Photoshop</p>
                </div>
            </div>
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-music"></i> Dance Styles</h5>
                    <p>Jazz, Street Jazz, R&B, Girl's Style</p>
                </div>
            </div>
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title"><i class="fas fa-video"></i> Content Creation</h5>
                    <p>Video editing, social media planning</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Connect Section -->
    <section id="connect">
        <h2 class="section-title" data-aos="fade-up"><i class="fas fa-envelope"></i> Connect with Me</h2>
        <div class="container" data-aos="fade-up" data-aos-delay="200">
            <div class="connect-links">
                <a href="https://behance.net/latuchukomol" target="_blank" rel="noopener noreferrer"><i class="fab fa-behance"></i> Behance</a>
                <a href="https://instagram.com/luchukomol" target="_blank" rel="noopener noreferrer"><i class="fab fa-instagram"></i> Instagram</a>
                <a href="mailto:bankki2fastpic@gmail.com"><i class="fas fa-envelope"></i> Email</a>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <p>"Difficult, not Impossible"</p>
    </footer>

    <!-- Suggested CDN: Bootstrap JS Bundle for interactive components like Carousel -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    
    <!-- Suggested CDN: AOS JS for on-scroll animations (initialize below) -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/aos/2.3.4/aos.js"></script>
    <script>
        AOS.init({
            once: true, // Ensures animations only happen once as you scroll down
            duration: 1000, // Animation duration
            easing: 'ease-out-cubic', // Easing function
        });

        // Enhanced scroll behavior for smooth navigation
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Lazy loading performance optimization
        if ('IntersectionObserver' in window) {
            const lazyImageObserver = new IntersectionObserver((entries, observer) => {
                entries.forEach((entry) => {
                    if (entry.isIntersecting) {
                        const lazyImage = entry.target;
                        lazyImage.src = lazyImage.dataset.src;
                        lazyImage.classList.remove('lazy');
                        lazyImageObserver.unobserve(lazyImage);
                    }
                });
            });

            const lazyImages = document.querySelectorAll('img[loading="lazy"]');
            lazyImages.forEach((lazyImage) => {
                lazyImageObserver.observe(lazyImage);
            });
        }

        // Enhanced carousel touch support for mobile
        const carousel = document.querySelector('#galleryCarousel');
        if (carousel && 'ontouchstart' in window) {
            let touchStartX = 0;
            let touchEndX = 0;

            carousel.addEventListener('touchstart', function(e) {
                touchStartX = e.changedTouches[0].screenX;
            });

            carousel.addEventListener('touchend', function(e) {
                touchEndX = e.changedTouches[0].screenX;
                handleGesture();
            });

            function handleGesture() {
                const swipeThreshold = 50;
                if (touchEndX < touchStartX - swipeThreshold) {
                    // Swipe left - next slide
                    bootstrap.Carousel.getInstance(carousel).next();
                }
                if (touchEndX > touchStartX + swipeThreshold) {
                    // Swipe right - previous slide
                    bootstrap.Carousel.getInstance(carousel).prev();
                }
            }
        }

        // Performance optimization: Pause animations when not in view
        const sections = document.querySelectorAll('section');
        const sectionObserver = new IntersectionObserver((entries) => {
            entries.forEach((entry) => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('in-view');
                } else {
                    entry.target.classList.remove('in-view');
                }
            });
        }, { threshold: 0.1 });

        sections.forEach((section) => {
            sectionObserver.observe(section);
        });
    </script>
</body>
</html>